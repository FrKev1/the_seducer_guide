{% extends 'front/front_layout.html.twig' %}

{% block title %}{{ parent() }} - article - {{ article.id }} {% endblock %}

{% block body %}
<h1>{{article.title}}</h1>
<div class="col-md-8 mx-auto">  
    <img class="d-block w-100 shadow p-1 mb-5 bg-white rounded" src=" /assets/images/uploads/{{ article.picture }}">
</div>
<div class="col mx-auto">
    {{ content|raw }}
    Article publié le : {{article.publicationDate|date('Y-m-d')}}
</div>
{% endblock %} 